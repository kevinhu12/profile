// Imports
import React from 'react';
import Container from '@mui/material/Container';

import { Navbar } from '../shared/Navbar';

// Styled components

// Main component
const Home = ({

}) => {
    
    return (
        <Container>
            <Navbar />
        </Container>
    );
}
export default Home;
